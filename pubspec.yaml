name: flame
description: "A modern dating app"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+10002



environment:
  sdk: ^3.10.7

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Navigation
  go_router: ^14.2.0

  # UI Components
  flutter_card_swiper: ^7.0.1
  cached_network_image: ^3.3.1
  flutter_animate: ^4.5.0

  # Utilities
  uuid: ^4.4.0
  intl: ^0.20.2
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.2.2
  image_picker: ^1.2.1
  record: ^6.2.0
  just_audio: ^0.9.42
  path_provider: ^2.1.4

  # Networking
  http: ^1.2.1
  socket_io_client: ^2.0.3+1
  web_socket_channel: ^3.0.1

  # Location
  geolocator: ^13.0.2
  # Reverse geocoding on the DEVICE. The alternative is shipping every user's
  # coordinates to a third-party geocoder from the server; the platform API
  # answers locally, so it does not leave the phone.
  geocoding: ^5.0.0
  permission_handler: ^11.3.1

  # Social Auth
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.4
  flutter_facebook_auth: ^7.1.1

  # Image processing
  image: ^4.7.2

  # Icons
  cupertino_icons: ^1.0.8

  # Push notifications
  firebase_core: ^4.14.0
  firebase_messaging: ^16.6.0

  # Splash Screen
  flutter_native_splash: ^2.4.4
  google_mlkit_face_detection: ^0.13.1
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Drives the typing-indicator timers deterministically; a real 5-second wait in
  # a test is both slow and flaky.
  fake_async: ^1.3.1

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # App Icon Generator
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  image_path_ios: "assets/icon/app_icon_ios.png"
  min_sdk_android: 21
  remove_alpha_ios: true
  # Android masks an adaptive icon into the launcher's shape and only the
  # central 66/108dp is guaranteed to survive. The foreground is therefore the
  # droplet alone on transparency, and the background is the brand gradient
  # with the droplet inpainted out — feeding the full square to both would
  # crop the droplet and stack it on a second copy of itself.
  adaptive_icon_background: "assets/icon/app_icon_background.png"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"

flutter_native_splash:
  # Sampled from the middle of the logo gradient. It was #E75A7C — the pink of
  # the previous brand — which flashed pink before an orange app every launch.
  color: "#CF461E"
  color_dark: "#CF461E"
  image: "assets/icon/splash_logo.png"
  image_dark: "assets/icon/splash_logo.png"
  android_12:
    color: "#CF461E"
    color_dark: "#CF461E"
    image: "assets/icon/splash_logo.png"
    image_dark: "assets/icon/splash_logo.png"
  ios: true
  android: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # App assets
  assets:
    - assets/images/
    - assets/images/social/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
